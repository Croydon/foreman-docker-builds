language: ruby

services:
  - docker

script:
  - set -e
  - docker --version
  - git clone --recurse-submodules https://github.com/theforeman/foreman foreman
  - cd foreman 
  - git checkout develop
  - docker build . --tag foreman 
